services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: designer-studio:latest
    container_name: designer-studio
    ports:
      - "8080:80"
    restart: unless-stopped
